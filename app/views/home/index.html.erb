<% content_for :fold do %>
  <div class="count">
    <strong><%= number_with_delimiter(@downloads_count) %> downloads</strong>
    of <%= number_with_delimiter(@rubygems_count) %> packages built since august 2011
  </div>
  <div class="blurb">
    <strong>Welcome to your community BPM host.</strong>
    <br />Find your packages easier, publish them faster, and have fun doing it.
  </div>
<% end %>

<div class="border">
  <div class="background">
    <div class="learn">
      <h3>learn</h3>
      <dl>
        <dt><%= link_to "Install BPM #{RUBYGEMS_VERSION}", page_url("download") %></dt>
        <dd>Ruby's premier packaging system</dd>
        <dt><%= link_to "Browse the Docs", "http://docs.rubygems.org" %></dt>
        <dd>The comprehensive guide on BPM</dd>
        <dt><%= link_to "BPM Specification", "http://docs.rubygems.org/read/chapter/20" %></dt>
        <dd>Your package's interface to the world</dd>
      </dl>
    </div>
    <div class="share">
      <h3>share</h3>
      <dl>
        <dt><code>gem install bpm --pre</code></dt>
        <dd>Update to the latest BPM version</dd>
        <dt><code>bpm pack</code></dt>
        <dd>Build your package</dd>
        <dt><code>bpm push foo-1.0.0.bpkg</code></dt>
        <dd>Deploy your package instantly</dd>
      </dl>
    </div>
  </div>
</div>

<p class="pitch">
  <strong><%= link_to t(:title), page_url("about") %></strong> is the BPM community's package hosting service.
  <strong><%= link_to "Instantly publish", page_url("gem_docs"), :anchor => "push" %></strong> your packages and install them.
  <strong><%= link_to "Use the API", page_url("api_docs") %></strong> to interact and find out more information about available packages.
  <strong><%= link_to "Become a contributor", "http://github.com/rubygems/gemcutter" %></strong> and enhance the site with your own changes.
</p>
<div class="leaderboards">
  <div class="grid_3" id="new_gems">
    <strong>New Packages</strong>
    <ol>
      <% @latest.each do |rubygem| %>
        <li>
          <%= link_to rubygem, rubygem, :title => short_info(rubygem.versions.most_recent) %>
        </li>
      <% end %>
    </ol>
  </div>
  <div class="grid_3" id="most_downloaded">
    <strong>Most Downloaded Today</strong>
    <% if @downloaded.any? %>
      <ol>
        <% @downloaded.each do |version, downloads| %>
          <li>
            <%= link_to "#{version.rubygem.name}-#{version.number} (#{number_with_delimiter downloads})", version.rubygem, :title => short_info(version) %>
          </li>
        <% end %>
      </ol>
    <% else %>
      <p>No downloads today.</p>
    <% end %>
  </div>
  <div class="grid_3" id="just_updated">
    <strong>Just Updated</strong>
    <ol>
      <% @updated.each do |version| %>
        <li>
          <%= link_to version.to_title, version.rubygem, :title => short_info(version) %>
        </li>
      <% end %>
    </ol>
  </div>
</div>
